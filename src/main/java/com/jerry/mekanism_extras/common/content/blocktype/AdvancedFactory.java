package com.jerry.mekanism_extras.common.content.blocktype;

/**
 * Compile-only stub for Mekanism Extras 1.20.1 AdvancedFactory class.
 * Used as @Mixin target class literal by ExtraFactoryMixin / ExtraFactoryForEMEMixin.
 * Runtime ME Extras jar either provides this class or the mixin fails silently (guarded).
 */
public abstract class AdvancedFactory {
    public AdvancedFactory() {}
}
